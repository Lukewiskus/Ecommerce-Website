import React from 'react';
import "./syles.scss";

const Header = props => {
    return (
        <header className="header">
            <div className="wrap">
                <div className="logo">
                    <h1>Logo</h1>
                </div>
            </div>
        </header>
    );
};

export default Header;